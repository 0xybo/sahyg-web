extends ./main.pug

append HEAD
	+meta("Confirm", "Confirm User")

	link(rel="stylesheet" href="/css/error.css") 

	title !{i18n("CONFIRMED")} | !{Web.config.get("text.title")}

block BODY
	div.content
		span.title !{i18n("CONFIRMED")}
		span.url
		span.description !{i18n("CONFIRMED_DESCRIPTION")}
		div.buttons
			a.login(href="/login?redirect=/")
				sahyg-button(rounded outline content=i18n("LOGIN"))
			a.home(href="/")
				sahyg-button(rounded outline contenti18n("RETURN_HOME"))
